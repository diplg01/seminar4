//  заданный массив
string str = "hdjhshjjhdb";
char c = 'w';
char[] massC = {'f', 'd'};

Console.WriteLine(str);
for (int i = 0; i < str.Length; i++)
{
    Console.Write(str[i] + " ");
}
Console.WriteLine();
for (int i = 0; i < massC.Length; i++)
{
    Console.Write(massC[i] + " ");
}